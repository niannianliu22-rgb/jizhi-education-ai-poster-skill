# 极致教育 AI 海报生成 Skill 使用说明

## 1. Skill 能做什么

本 Skill 用于生成以下三个品牌的教育宣传海报：

- 极致 Essay
- DP / Distinction Pass
- 极致 AI 留学生陪跑

支持产品介绍、服务流程、产品对比、客户案例、活动宣传、课程推广等海报类型。Skill 会自动调用对应品牌的 Logo、色系、IP 人物和成功海报参考，并执行品牌隔离与素材检查。

## 2. 安装方法

1. 解压收到的 `jizhi-education-ai-poster-share.zip`。
2. 在访达中按 `Command + Shift + G`。
3. 输入 `~/.codex/skills/` 并回车。
4. 将解压后的整个 `jizhi-education-ai-poster` 文件夹放入该目录。
5. 完全退出并重新打开 Codex。

安装完成后的正确路径：

```text
~/.codex/skills/jizhi-education-ai-poster/SKILL.md
```

不要形成两层同名文件夹，例如：

```text
~/.codex/skills/jizhi-education-ai-poster/jizhi-education-ai-poster/SKILL.md
```

## 3. 最简单的使用方法

在 Codex 新任务中直接输入：

```text
使用 $jizhi-education-ai-poster。
为 DP 生成一张产品介绍海报，直接生成最终海报，不要只输出文案方案。

海报内容：
【在这里粘贴产品信息】
```

如果输入框无法识别 `$jizhi-education-ai-poster`，也可以写：

```text
请使用“极致教育 AI 海报生成 Skill”完成下面的海报任务。
```

## 4. 推荐需求模板

复制下面的模板并填写：

```text
使用 $jizhi-education-ai-poster，直接生成最终海报。

品牌：DP / 极致 Essay / 极致 AI 留学生陪跑
海报类型：产品介绍 / 服务流程 / 产品对比 / 客户案例 / 活动宣传
使用场景：朋友圈 / 小红书 / 社群 / 公众号
海报内容：
【粘贴完整内容】

必须保留的信息：
【价格、课时、周期、成绩、学校、专业等】

图片素材：
【如有，请在当前对话上传原图】

其他要求：
【尺寸、风格、是否使用 IP 等；没有可不填】
```

如果没有指定尺寸，Skill 会根据内容量选择合适的竖版比例；案例海报长度由内容和截图数量决定，不会默认生成长图。

## 5. 三个品牌的使用示例

### DP 产品海报

```text
使用 $jizhi-education-ai-poster，为 DP 生成一张“卓越安心包”产品介绍海报。
使用 DP 品牌色和胸口带 DP 标识的 IP 人物，直接生成最终海报。

产品内容：
【粘贴产品信息】
```

### 极致 Essay 流程海报

```text
使用 $jizhi-education-ai-poster，为极致 Essay 生成一张“全课程包过辅导流程”海报。
保持信息清晰、年轻、有营销感，直接生成最终海报。

流程内容：
【粘贴流程信息】
```

### 极致 AI 留学生陪跑对比海报

```text
使用 $jizhi-education-ai-poster，为极致 AI 留学生陪跑生成一张“传统辅导 VS AI 陪跑”服务对比海报。
使用 AI 陪跑 Logo 和胸口带 AI 标识的 IP 人物，突出规划、跟进、检测和复盘，直接生成最终海报。

对比内容：
【粘贴对比信息】
```

## 6. 客户案例海报怎么使用

制作客户案例海报时，除了案例文字，还需要在当前对话上传聊天截图、成绩截图等原始证据图片。

推荐输入：

```text
使用 $jizhi-education-ai-poster，为 DP 生成一张客户案例海报，直接生成最终海报。

标题：辅导好评档案
学员信息：格拉斯哥大学、航空航天、硕士
辅导前情况：专业课难度高，对知识框架掌握不完整，担心多门课程无法顺利通过。
匹配师资：南安普顿大学、航空航天工程、硕士
辅导过程：导师带领学生理清专业知识底层逻辑。
辅导成果：六门课全部通过。

我已上传学员反馈截图。截图必须使用原图，只允许等比例缩放和排版，不得修改、重绘或重新生成。
```

案例图片规则：

- 聊天截图、成绩截图和付款截图必须使用用户当次上传的原图。
- 不修改截图文字，不重绘聊天界面，不生成替代截图。
- 如截图包含姓名、电话、账号等隐私，应先由提供者在原图中完成脱敏，再上传使用。
- Skill 压缩包不附带历史客户案例证据，新案例必须单独上传。

## 7. 如何修改生成结果

生成后可以直接提出具体修改，不需要重新描述全部内容。

```text
保留当前版式，只把主标题放大，并减少正文密度。
```

```text
保留所有文字和原始截图，改成更高级的 DP 深紫色风格。
```

```text
IP 人物背景与海报背景分层明显，请改用透明底素材重新排版，不要重绘 IP。
```

```text
删除所有 CTA、按钮、二维码引导和咨询提示，其他内容不变。
```

## 8. 已锁定的品牌规则

- 所有品牌、所有海报默认不添加 CTA。
- DP 只能使用 DP Logo 和胸口带 DP 标识的 IP。
- 极致 AI 留学生陪跑只能使用对应 Logo 和胸口带 AI 标识的 IP。
- 两个品牌的 IP 不得混用。
- Logo 和 IP 使用 Skill 内置原始素材，不进行 AI 重绘。
- 成功海报案例只用于参考版式、层级、信息密度和视觉风格。
- 历史案例中的文案、价格、承诺、CTA 和客户信息不会自动复用。
- 不捏造产品、价格、成绩、客户评价或服务承诺。

## 9. 常见问题

### 为什么只生成了文案，没有生成海报？

在需求开头明确写：

```text
直接生成最终海报，不要只输出文案方案。
```

### 为什么没有使用客户聊天截图？

历史案例截图不会随 Skill 提供。制作案例海报时，需要在当前对话重新上传本次案例的原始截图。

### 为什么没有 IP 人物？

可以补充：

```text
请使用当前品牌对应的内置 IP 原始素材。
```

如果没有适合当前构图的透明底素材，Skill 会优先不使用，而不是混用其他品牌 IP。

### 安装后无法识别 Skill 怎么办？

检查以下项目：

- Codex 是否已经完全退出并重新打开。
- 文件夹是否位于 `~/.codex/skills/jizhi-education-ai-poster/`。
- `SKILL.md` 是否直接位于该文件夹中。
- 是否误放成两层同名文件夹。
